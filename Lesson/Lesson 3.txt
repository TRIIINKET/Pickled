PHP LIBRARIES

Prepared by:

Dr. MARIENEL N. VELASCO

TOPICS:

1. PHP extensions
2. Text Functions
3. Testring String Values
4. Date and Time Functions
5. Image Handling Functions

.

PHP extensions
• You can view which extensions are actively installed in your specific
PHP server environment by using the special phpinfo().
TRY IT!
Program#1 (extensions.php)

.

Splitting Strings
• The str_getcsv() is extremely useful when you need to parse
comma-separated data entered by site visitors, such as search.
terms.
TRY IT!
Program#2
(parseinput.html)
and
(parseoutput.php)

.

Testing String Values

TRY IT!
Program#3
(inputvalidation.php)

.
trim() → removes extra spaces
empty() → checks if input is blank
preg_match() → checks if input follows a pattern
^[a-zA-Z ]*$ → only letters and spaces allowed

Testing String Values (cont)

TRY IT!
Program#4
(strcmp.php)
and
(strcasecmp.php)

.
• strcmp() – compares the binary value of two strings.
• strcasemp() – performs a case sensitive string functions.

Testing String Values (cont)

TRY IT!
Program#5
(datatype.php)

.
• is_bool– returns a TRUE value if the string is a valid

Boolean value.

• is_float – returns a TRUE value if the string is a valid

float value.

• is_int() - returns a TRUE value if the string is a valid

integer value.

• Is_null() - returns a TRUE value if the string is a NULL
value.
• is_numeric - returns a TRUE value if the string is a valid

number or numeric value.

Testing String Values (cont)

TRY IT!
Program#6
(str.php)

.
• str_word_count– returns the number of words in a string, or an array of the words
• strlen– returns the number of characters in a string.

Testing String Values (cont)

TRY IT!
Program#7
(strpos.php)

.
strpos() – use to search a specific value within a string.

Date and Time Functions

TRY IT!
Program#8
(date.php)

.
date() – used to generate human-readable dates and times.

Date and Time Functions (cont)

TRY IT!
Program#9
(time.php)

.
strtotime() – converts a date/time string value in just about any format into as timestamp value

• .

Image Handling Functions (cont.)
TRY IT!
Program#10
(imageupload.html)
and
(imageconvert.php)